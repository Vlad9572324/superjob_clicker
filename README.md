# SuperJob Auto-Apply

Автоматический отклик на вакансии superjob.ru с сопроводительным письмом.

## Установка

```bash
# Клонируем / переходим в папку
cd superjob

# Устанавливаем зависимости
pip install -r requirements.txt
```

## Настройка

### 1. Авторизуйтесь на superjob.ru

Откройте браузер (Firefox или Chrome) и войдите в аккаунт на https://www.superjob.ru

### 2. Настройте `.env`

```env
# Браузер для извлечения кук
# auto = пробует: firefox → chrome → edge → safari
# Рекомендуется: firefox (стабильнее всего)
BROWSER=auto

# ID вашего резюме (из URL: superjob.ru/resume/XXXXXX.html)
RESUME_ID=12345678

# Ключевые слова для поиска (через запятую)
SEARCH_KEYWORDS=менеджер,секретарь,офис-менеджер

# Вакансий на страницу
SEARCH_LIMIT=20

# Сколько страниц загружать (глубина поиска)
# 20 вакансий × 3 страницы = до 60 вакансий на keyword
MAX_PAGES=3

# Сопроводительное письмо (\n = перенос строки)
COVER_LETTER=Здравствуйте!\n\nМеня заинтересовала ваша вакансия...\n\nС уважением,\nИмя
```

### 3. Узнайте ID резюме

1. Откройте https://www.superjob.ru/user/resume/
2. Нажмите на своё резюме
3. В URL будет: `superjob.ru/resume/56233085.html` → ID = `56233085`

## Запуск

```bash
python superjob_auth.py
```

### Вывод:

```
=== SuperJob Auto-Apply ===
Browser: auto
Resume ID: 56233085
Keywords: менеджер, секретарь
Limit: 20 вакансий/страница x 3 страниц = до 60 на keyword

🔄 Проверяю кеш... ✅ куки валидны
✅ Авторизация успешна!

🔍 Поиск: менеджер
   Найдено: 150 вакансий, загружаем до 60
  ✅ 💬 Офис-менеджер @ ООО Компания
  ✅ 💬 Менеджер по продажам @ АО Фирма
  ❌ Уже откликались ранее

📊 Итого: отправлено 45, ошибок 3, пропущено 12

💾 Результаты сохранены в results.json
```

## Использование как библиотеки

```python
from superjob_auth import SuperjobAPI

api = SuperjobAPI()
api.auth_from_browser()  # или auth_from_browser("firefox")

# Поиск вакансий
result = api.search_vacancies("python", limit=10)
for v in result["vacancies"]:
    print(f"{v['title']} @ {v['company']}")

# Отклик на вакансию (с сопроводительным из .env)
result = api.apply_to_vacancy(
    vacancy_id="51234567",
    resume_id="56233085"
)
print(f"Chat: {result['chat_url']}")

# Или со своим письмом
result = api.apply_to_vacancy(
    vacancy_id="51234567",
    resume_id="56233085",
    cover_letter="Своё письмо..."
)

# Автоматический отклик по всем keywords из .env
stats = api.auto_apply()
```

## Файлы

| Файл | Описание |
|------|----------|
| `.env` | Настройки (keywords, resume_id, письмо) |
| `.superjob_cookies.json` | Кеш кук (создаётся автоматически) |
| `cookies.txt` | Ручной ввод кук (backup) |
| `results.json` | Результаты последнего запуска |

## Ручная авторизация (если браузер не работает)

1. Откройте superjob.ru → F12 → Application → Cookies
2. Скопируйте значения `uat`, `sat`, `sask` в `cookies.txt`:

```
uat=xxxxx
sat=xxxxx
sask=xxxxx
```

3. Измените код запуска:

```python
api = SuperjobAPI()
api.auth_from_file("cookies.txt")
```

## Кроссплатформенность

| Браузер | Windows | macOS | Linux |
|---------|:-------:|:-----:|:-----:|
| Firefox | ✅ | ✅ | ✅ |
| Chrome | ✅ | ⚠️* | ✅ |
| Edge | ✅ | ✅ | ❌ |
| Safari | ❌ | ✅ | ❌ |

*На macOS Chrome может требовать закрытия браузера

**Рекомендация:** используйте Firefox — работает стабильнее всего.

## Troubleshooting

### "Unable to get key for cookie decryption"
- Закройте браузер и попробуйте снова
- Или используйте Firefox: `BROWSER=firefox`

### "Не найдены куки в браузере"
- Авторизуйтесь на superjob.ru в браузере
- Проверьте что выбран правильный браузер

### "Куки невалидны"
- Сессия истекла, перелогиньтесь на superjob.ru
- Удалите `.superjob_cookies.json` и запустите снова